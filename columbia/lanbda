import random
#lambda 
lower = lambda string: string.lower()
exponent = lambda num, root: num**root

#list for loops
my_string = 'the fox jumped over the lazzy dog'
my_list = my_string.split(' ')
my_list2 = [x.title() for x in my_list]

nums = [x for x in range(0,11)]
nums = ['even' if x%2<1 else 'odd' for x in nums]
print(nums)

#hw
rand_list = [random.randint(0, 100) for x in range(0,11)]
def sorter(list):
    return list.sort()
#or 
    #for i in list:
        #append i 
ex = {
    'First': 'Marco',
    'last': 'Rosenberg',
    'aGE': '15'
}
def lower_dict(dict): #important
    new_dict = {}
    for key in dict:
        new_dict[key.lower()] = dict[key]
    return new_dict



